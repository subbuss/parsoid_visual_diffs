* npm install
* Add $PWD/node_modules/phantomjs/bin to PATH

node gen.screenshots.js --help (to see options)
node gen.visual_diff.js <prefix> (dumps <prefix>.diff.jpg to current directory)

gen.visual_diff.js assumes that base64 is in /usr/bin/base64. To be cleaned up.
